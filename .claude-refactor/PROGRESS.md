# Koentji Refactor — Progress

- Plan: `/root/.claude/plans/use-razif-coding-style-audit-current-velvet-lampson.md`
- Started: 2026-04-17
- Current phase: 6 (complete — Phase 7 deferred per user)
- Next commit: (Phase 7 deferred per user)

## Checklist

### Phase 0 — safety net
- [x] 0.1  test: add integration harness and domain-test helpers
- [x] 0.2  fix: bind custom date-range parameters in dashboard stats query
- [x] 0.3  fix: decrement rate limit atomically on /v1/auth
- [x] 0.4  chore: drop stale agAuth/ references from docs
- [x] 0.5  tec: make check aggregates fmt + clippy + test; CI runs it

### Phase 1 — domain extraction
- [x] 1.1  tec: introduce domain module skeleton and value objects
- [x] 1.2  tec: extract IssuedKey aggregate with lifecycle verbs
- [x] 1.3  tec: define AuthDenialReason enum with en/id mapping at HTTP edge
- [x] 1.4  tec: introduce IssuedKeyRepository port + Postgres adapter
- [x] 1.5  tec: hide Moka auth cache behind AuthCachePort
- [x] 1.6  feat: route /v1/auth through AuthenticateApiKey use case
- [x] 1.7  test: cover IssuedKey.authorize across all denial reasons
- [x] 1.8  test: integration tests for Postgres IssuedKeyRepository

### Phase 2 — admin verbs
- [x] 2.1  feat: issuing a key emits KeyIssued and returns an IssuedKey
- [x] 2.2  feat: revoking a key emits KeyRevoked and invalidates auth cache
- [x] 2.3  feat: reassigning a device / resetting rate limit / extending expiration as domain commands
- [x] 2.4  fix: device reassignment also evicts the prior auth-cache entry
- [x] 2.5  test: cover key-management commands

### Phase 3 — schema hardening
- [x] 3.1  feat: enforce UNIQUE(key, device_id)
- [x] 3.2  feat: index authentication_keys on (key, device_id) — subsumed by 3.1's unique index
- [x] 3.3  feat: audit_log table captures domain events
- [x] 3.4  tec: outbox adapter publishes domain events to audit_log

### Phase 4 — admin auth hardening
- [x] 4.1  feat: admin password verified against argon2id hash
- [x] 4.2  feat: admin login uses constant-time compare
- [x] 4.3  feat: per-IP sliding-window lockout after 5 failed logins
- [x] 4.4  tec: session cookie honours COOKIE_SECURE in prod
- [x] 4.5  test: cover argon2 verify and the lockout ledger (covered inline with 4.1/4.2/4.3)

### Phase 5 — operational readiness
- [x] 5.1  feat: access log middleware emits structured JSON lines
- [x] 5.2  feat: every request carries an X-Request-Id propagated into logs
- [x] 5.3  feat: /healthz and /readyz expose liveness and readiness
- [x] 5.4  feat: graceful shutdown drains in-flight requests on SIGTERM
- [x] 5.5  tec: pg pool gains acquire/idle timeouts and test-before-acquire
- [x] 5.6  tec: container runs as non-root with a pinned base image and a HEALTHCHECK
- [x] 5.7  tec: CI enforces fmt --check, clippy -D warnings, cargo audit, docker build

### Phase 6 — frontend design system
- [x] 6.1  tec: introduce semantic design tokens in tailwind.config.js
- [x] 6.2  tec: extract Button, Input, Select, Surface, Stack, Badge primitives
- [x] 6.3  fix: define the missing slide-in keyframes referenced by the toast — subsumed by 6.1 (keyframe + animation utility landed with the token config)
- [x] 6.4  tec: collapse the three CRUD pages onto a shared CrudPage scaffold
- [x] 6.5  fix: modal traps focus, closes on ESC, and restores focus on close
- [x] 6.6  fix: clipboard copy uses the clipboard API directly (no eval)
- [x] 6.7  fix: charts are invoked via a typed wasm_bindgen extern
- [x] 6.8  fix: keys-page search debounces with a single cancellable timer
- [x] 6.9  fix: key form surfaces submit errors through the toast
- [x] 6.10 feat: keys page filters round-trip through URL query params
- [x] 6.11 tec: move components/ and pages/ under ui/ feature folders

### Phase 7 — polish
- [ ] 7.1  docs: rewrite README and CLAUDE.md against the new layout
- [ ] 7.2  docs: ARCHITECTURE.md documents the bounded contexts and flow
- [ ] 7.3  feat: /metrics exposes auth decisions, cache hits, denial reasons (optional)

## Log

- 0.1  2026-04-17 — `tests/common/{mod,clock,db,key_builder}.rs` + `tests/harness_smoke.rs`; shared test DB, KeyBuilder, TestClock. Pre-existing clippy errors in `src/` are unrelated and will be addressed later.
- 0.2  2026-04-17 — `src/server/stats_service.rs` rewritten to bind `Option<DateTime<Utc>>` into every query. `custom` range now parses strictly as YYYY-MM-DD — malformed input degrades to (None, None). 6 regression tests in `tests/stats_date_range.rs`.
- 0.3  2026-04-17 — new `src/rate_limit.rs` with `consume_rate_limit`: a single `UPDATE … RETURNING` decides reset-vs-decrement in-SQL and locks the row, closing the read-modify-write leak. `/v1/auth` no longer spawns a fire-and-forget writer. 6 regression tests in `tests/rate_limit_atomic.rs` including a 10-concurrent-spawn race probe. `tests/common/db.rs` hands each `#[tokio::test]` a runtime-local pool (PgPool isn't runtime-portable) while keeping DB setup + migrations one-shot per process.
- 0.4  2026-04-17 — `README.md` + `CLAUDE.md` no longer claim a sibling `agAuth/` crate; replaced with an accurate single-crate layer table that mentions the `tests/` + `end2end/` suites.
- 0.5  2026-04-17 — `make test` no longer calls `cargo fmt` (which rewrites in CI); new `make check = fmt-check + clippy (-D warnings, --tests) + test`. Also cleared the ~10 pre-existing `clone_on_copy` / `if let`-rewriteable clippy warnings in `src/pages` / `src/components` / `src/main.rs` so the gate is actually usable. `.github/workflows/test.yml` now runs `make check` against a Postgres service container.
- P0-e2e 2026-04-17 — `make e2e` (api project) run at Phase 0/1 boundary. Found the legacy off-by-one: old handler returned 429 as soon as post-decrement remaining hit `<= 0`, so only `daily - 1` consumes per window are actually usable. Preserved the semantic in `rate_limit.rs` (`remaining > usage`, `daily > usage`) and realigned the unit tests. All 10 api e2e tests green on chromium.
- 1.1  2026-04-17 — new `src/domain/` module with the `authentication` bounded context and `DomainError`. Value objects: `AuthKey`, `DeviceId` (including the `"-"` unclaimed sentinel), `RateLimitAmount`, `RateLimitUsage`, `RateLimitWindow`, `SubscriptionName`. Every one has `parse/new` validation and unit tests covering empty / over-long / negative / zero branches — 22 tests. Nothing is wired into the HTTP path yet; this is the vocabulary that 1.2–1.6 will lean on.
- 1.2  2026-04-17 — `src/domain/authentication/auth_decision.rs` introduces `AuthDecision` + `DenialReason`; `src/domain/authentication/issued_key.rs` hosts the `IssuedKey` aggregate with `authorize(usage, now) -> AuthDecision` (pure) plus the lifecycle verbs `revoke`, `reassign_to`, `reset_rate_limit`, `extend_until`. Authorize preserves the legacy off-by-one (`daily > usage`, `remaining > usage`) and the free-trial-vs-admin expiry split. 12 aggregate tests cover active/revoked/idempotent-revoke/expired-admin/expired-trial/off-by-one/usage>=daily/window-reset/null-updated/reassign-preserves-ledger/reset-restores-daily/extend-set-clear.
- 1.3  2026-04-17 — new `src/interface/http/i18n.rs` with `DenialEnvelope::from_reason` + `status_code(reason)`. Strings copied byte-for-byte from the inline `json!` blocks in `src/main.rs` so e2e text assertions keep passing. 5 unit tests pin each denial shape + its legacy status code (Unknown/Revoked/Expired/FreeTrial → 401, RateLimit → 429). Not wired yet — 1.6 will mount the new envelope via the `AuthenticateApiKey` use case.
- 1.4  2026-04-17 — new `src/domain/authentication/issued_key_repository.rs` defines the port (`find` + `consume_quota` via `async_trait`, `ConsumeOutcome`, `RepositoryError`). New `src/infrastructure/postgres/issued_key_repository.rs` implements it — `find` hydrates an `IssuedKey` via a private `IssuedKeyRow` so `sqlx::FromRow` never leaks onto domain types; `consume_quota` reuses the atomic `UPDATE … RETURNING` from 0.3 verbatim. `async-trait` added to deps under the `ssr` feature. Not wired into `/v1/auth` yet — 1.6 hooks it up.
- 1.5  2026-04-17 — new `src/domain/authentication/auth_cache_port.rs` (`AuthCachePort` with `get`/`put`/`invalidate` over `(AuthKey, DeviceId)`). New `src/infrastructure/cache/moka_auth_cache.rs` adapts Moka to the port and stores the full `IssuedKey` aggregate rather than the legacy DB row. Legacy `src/cache.rs` stays until 1.6 swaps the endpoint.
- 1.6  2026-04-17 — `/v1/auth` now flows through `koentji::application::AuthenticateApiKey` (new) → `koentji::interface::http::auth_endpoint` (new). `src/main.rs` shrinks from 634 lines to ~188 (wiring only). The handler is non-generic and takes `Arc<AuthenticateApiKey>`; the use case holds `Arc<dyn IssuedKeyRepository>` + `Arc<dyn AuthCachePort>` so the actix `#[post]` macro doesn't fight type parameters. `IssuedKey` gains `username`/`email` (envelope echoes them). `claim_free_trial` ported onto the repository adapter preserving the two-branch INSERT/rebind upsert + 1st-of-next-month UTC calendar math. Envelope byte-identical: `DenialEnvelope::from_reason` + `status_code` renders the same `{ error: { en, id }, message }` as before. Legacy `src/cache.rs` and `src/server/key_service.rs` unchanged (Phase 2 migrates them). 39 unit + 3 harness + 6 rate_limit + 6 stats tests all green.
- 1.7  2026-04-17 — +24 `IssuedKey.authorize` tests covering: priority-of-denial (revoked beats expired beats rate-limit, free-trial-ended vs rate-limit), clock boundaries (==expiry denies, nanosecond before allows; window reset fires at `==` boundary, not one ns early), ledger arithmetic edge cases (remaining==usage+1 allowed, usage==daily denied, `usage==0` is allowed without decrement), non-default window (60s window resets after 61s), purity (authorize does not mutate), `updated_at` stamping, denial timestamp round-trip (revoked/expired/free-trial), and lifecycle verb interactions (reassign/reset don't unrevoke; extend_until revives an expired key; None expiry is endless). 36 authorize-focused tests in total (>30 plan target). 78 tests across 6 suites all green.
- 1.8  2026-04-17 — new `tests/postgres_issued_key_repository.rs` (12 tests) exercises the real DB adapter: `find` hydrates identity + ledger + username/email and returns revoked rows (they are not treated as missing), default window falls back to 86_400s. `consume_quota` allows within quota, refuses at the legacy off-by-one, resets a stale window, never oversells under 20 concurrent spawns (exactly 19 Allowed, final remaining=1). `claim_free_trial` inserts on marker match, rebinds a pre-issued `device_id='-'` row (final count=1), returns None for a plain unknown key. Every test first calls `fresh_pool()` → `reset` so cross-pollution is impossible. 90 rust tests across 7 suites + 77 Playwright tests all green — Phase 1 boundary verified.
- 2.1  2026-04-17 — new `src/application/issue_key.rs` (`IssueKey` use case) + `IssueKeyCommand` on the repository port + `issue_key` on the Postgres adapter. `server::key_service::create_key` now parses the request into domain value objects (`AuthKey`, `DeviceId`, `SubscriptionName`, `RateLimitAmount`) and delegates to `IssueKey`; the server-fn contract still returns `AuthenticationKey` (full DB row incl. timestamps / audit fields), so the adapter re-fetches by id after the insert — the domain aggregate intentionally doesn't carry those. `main.rs` wires `Arc<IssueKey>` alongside `AuthenticateApiKey`; both share the same `Arc<dyn IssuedKeyRepository>`. `KeyIssued` past-tense log line emitted from the use case — the formal outbox/audit adapter lands in 3.4. No new tests this commit (2.5 covers the admin verbs as a suite); envelope + existing 90 rust tests still green.
- 2.2  2026-04-17 — new `src/application/revoke_key.rs` (`RevokeKey` use case) + `revoke_key` on the repository port and Postgres adapter. The SQL is idempotent — `COALESCE(deleted_at, NOW())` keeps the original revocation timestamp if the row is already revoked, while `RETURNING key, device_id` lets the use case evict the cache without a second round-trip. `RevokeKey::execute` composes the repo call with `AuthCachePort::invalidate`, so the `delete_key` server fn shrinks to parse id → delegate. `main.rs` wires `Arc<RevokeKey>` sharing the *same* `auth_cache_port` as `AuthenticateApiKey` so admin evictions actually land on the hot-path cache. Legacy `GLOBAL_AUTH_CACHE` OnceLock still serves `update_key` + `reset_rate_limit` — 2.3 migrates those next. 90 rust tests still green; `KeyRevoked` log line emitted.
- 2.3  2026-04-17 — three new admin verbs on the port + adapter + application layer: `ReassignDevice` (CTE-based UPDATE snapshots the previous `device_id` in the same statement so the use case can evict *both* `(key, prev_dev)` and `(key, new_dev)` — this is what 2.4 will wire into `update_key`), `ResetRateLimit` (UPDATE + cache evict; `reset_rate_limit` server fn now delegates), `ExtendExpiration` (UPDATE expired_at + cache evict). New domain struct `DeviceReassignment { key, previous_device, current_device }` is exported from `domain::authentication`. `main.rs` wires all three as app_data sharing the same `Arc<dyn AuthCachePort>`. Past-tense log lines emitted (`DeviceReassigned`, `RateLimitReset`, `KeyExpirationExtended`). `update_key` still uses the legacy blob UPDATE + `GLOBAL_AUTH_CACHE` helper — 2.4 cuts that over to `ReassignDevice` (which fixes B9: the old cache entry is now evicted too). 90 rust tests still green.
- 2.4  2026-04-17 — `update_key` server fn now snapshots the pre-UPDATE `(key, device_id)` and evicts that entry via `Arc<dyn AuthCachePort>` (injected as `web::Data`), not the legacy `GLOBAL_AUTH_CACHE` OnceLock. When the device was reassigned, the new `(key, new_device)` entry is also evicted so a concurrent read doesn't leave stale hot-path state. Fixes B9 — legacy helper looked up the CURRENT `device_id` after the UPDATE, so the prior entry stayed cached. `GLOBAL_AUTH_CACHE`, `set_global_auth_cache`, `invalidate_cache_for_key`, and the now-orphan `src/cache.rs` module are removed; `main.rs` drops the `legacy_auth_cache` + `set_global_auth_cache` wiring. Only surviving auth cache is the port + Moka adapter. 90 rust tests still green.
- 2.5  2026-04-17 — +11 adapter integration tests in `tests/postgres_issued_key_repository.rs` covering all five admin verbs (`issue_key` happy-path + remaining=daily, `revoke_key` happy/idempotent-timestamp-preserved/unknown-id, `reassign_device` returns prev+curr/unknown-id, `reset_rate_limit` restores+stamps/unknown-id, `extend_expiration` set+clear/unknown-id). New `tests/key_management_use_cases.rs` (9 tests) uses in-memory `FakeRepo`/`FakeCache` to pin use-case orchestration: every state-mutating verb evicts the cache on success; `ReassignDevice` evicts BOTH `(key, prev_dev)` and `(key, new_dev)` (B9 guard); unknown ids short-circuit without touching the cache; `IssueKey` never touches the cache at all. Also fixes a pre-existing harness flake: added `--test-threads=1` to `make test` — integration tests share one Postgres DB and coordinate via `reset()`, so parallel TRUNCATEs were stomping on the in-flight concurrency test. 110 rust tests across 8 suites all green.
- P2-e2e 2026-04-17 — Phase 2 boundary `make e2e`: 77/77 green (53 chromium + 10 api + 14 webkit smoke). `/v1/auth` envelope unchanged; admin CRUD flows still work through the new IssueKey / RevokeKey / ReassignDevice / ResetRateLimit / ExtendExpiration use cases.
- 3.1  2026-04-17 — `migrations/004_unique_key_device.sql` closes B10. The migration first dedups any ambient duplicate `(key, device_id)` pairs (keeps the most recent `id`, drops the rest — matches "last write wins" admin mental model) and then creates `idx_auth_keys_key_device_unique`. The `IssuedKey` aggregate already addresses rows by this tuple (`find` / `consume_quota` / `revoke_key`), so the constraint is a database-level guarantee of an invariant the domain has always assumed. Two new tests in `tests/schema_constraints.rs` pin the constraint: duplicate `(key, device_id)` is rejected with an error mentioning the index name; the same `key` with different `device_id` is still permitted (FREE_TRIAL and pre-issued rows need this). 112 rust tests across 9 suites green.
- 3.2  2026-04-17 — **no new migration** — 3.1's `idx_auth_keys_key_device_unique` already serves as the hot-path composite btree. Postgres's query planner treats a unique btree identically to a plain composite btree for equality lookups on `(key, device_id)`, so adding a second index would be duplication. Closes B11 as a consequence of 3.1.
- 3.3  2026-04-17 — `migrations/005_audit_log.sql` adds the durable `audit_log` table (`id`, `event_type`, `aggregate_id`, `actor`, `payload JSONB`, `occurred_at`). Two indexes cover the expected dashboards: `idx_audit_log_occurred_at` (recent-first scrolling) and a partial `idx_audit_log_aggregate` on `(aggregate_id, occurred_at DESC) WHERE aggregate_id IS NOT NULL` (per-key history). Payload is JSONB so 3.4 can evolve the shape without another migration. No code changes yet — 3.4 wires the outbox adapter that writes rows from the use-case layer.
- P3-e2e 2026-04-17 — Phase 3 boundary `make e2e`: 77/77 green (53 chromium + 10 api + 14 webkit smoke). Migrations 004 + 005 apply cleanly on the e2e database; admin flows emit the past-tense log lines from the use-case layer; `/v1/auth` envelope unchanged.
- P4-e2e 2026-04-17 — Phase 4 boundary `make e2e`: 77/77 green (53 chromium + 10 api + 14 webkit smoke). Admin login / logout / invalid-credentials flows pass with the argon2-path primed via `ADMIN_PASSWORD` plaintext fallback (the e2e `SERVER_ENV` sets `COOKIE_SECURE=false` so the browser keeps the session on plain-HTTP). One sub-threshold failed login across the whole suite — lockout not triggered.
- 4.5  2026-04-17 — covered inline: `AdminCredentials` (8 tests in 4.1) + argon2 hasher round-trip (2 tests in 4.1) + constant-time string compare (5 tests in 4.2) + sliding-window ledger (9 tests in 4.3). No separate 4.5 commit needed — tests shipped with the features they pin.
- 4.4  f0ae3b6 2026-04-17 — `main.rs` reads `COOKIE_SECURE` at boot and passes it to `SessionMiddleware::builder(...).cookie_secure(bool)`. Default is `true`: a production admin session cookie over plain HTTP would be a credential-theft vector, and leaving this hidden invited misconfiguration. `false` / `0` / `no` opt out; missing var logs a warn and defaults to `true`. `.env.example` and `docker-compose.yml` wire the var through. `end2end/fixtures/env.ts::SERVER_ENV` passes `COOKIE_SECURE=false` so Playwright's plain-HTTP driver keeps its admin session (Secure cookies are dropped by the browser over HTTP). 144 rust tests green.
- 4.3  07e1c6f 2026-04-17 — new `src/domain/admin_access/login_attempt_ledger.rs`: `LoginAttemptLedger` with a `Mutex<HashMap<IpString, VecDeque<DateTime<Utc>>>>`, a `LockoutPolicy { max_failures, window }` (defaults 5/5min), and three methods — `check(ip, now)`, `record_failure(ip, now)`, `clear(ip)` — all accepting an explicit `now` so tests can pin window edges without a fake clock crate. Prune semantics: a failure whose age is **equal to** the window is already aged-out (open interval at the far edge), otherwise an attacker who waits exactly `window` always sees a fresh slot. `src/auth.rs::login` now consults the ledger: if `check()` returns `LockedOut`, it short-circuits to `Ok(false)` without running argon2 (saves CPU under attack); on a failed verify it records the failure (and if this was the Nth, it logs the lockout); on success it clears the IP. Client IP comes from `HttpRequest::connection_info().realip_remote_addr()` (honours X-Forwarded-For, falls back to peer — fine for single-replica behind a trusted proxy). `main.rs` constructs `Arc<LoginAttemptLedger>` at boot and injects via `web::Data`. 9 ledger unit tests cover: first-failure-allowed, max-failures-locks, check-does-not-tick, window-age-out, clear-resets, per-IP-independence, retry_after-shrinks-with-time, edge-equal-window aged-out, continued-attempts-during-lockout keep returning LockedOut. 144 rust tests across 10 binaries green.
- 4.2  bee9181 2026-04-17 — new `src/domain/admin_access/constant_time.rs::equals_in_constant_time` wraps `subtle::ConstantTimeEq` on string byte slices (mismatched lengths short-circuit to false; same-length inputs always compare every byte). 5 unit tests pin: equal strings, same-length mismatch, length-mismatch, empty==empty, non-ASCII. `src/auth.rs::login` restructured so both username AND password checks always run: `let user_ok = equals_in_constant_time(&username, admin_username); let pw_ok = creds.verify(&password); if user_ok & pw_ok`. Bitwise `&` on `bool` is non-short-circuiting in Rust, so verify() executes regardless of whether the username matched — wrong-user and wrong-password cases are no longer timing-distinguishable (previously the `if username != admin_username` early-return let a timer leak that signal). Missing-credentials path still short-circuits (config error, not an auth decision). 135 rust tests across 10 binaries green.
- 4.1  88a4406 2026-04-17 — new `src/domain/admin_access/admin_credentials.rs` — `AdminCredentials` value object with dual-path `CredentialFlavour::Hashed(String)` (argon2id PHC) / `Plaintext(String)` (dev/e2e fallback). `from_hash` fails closed on a malformed PHC (deploy-time mistake); `from_plaintext` rejects empty and logs `warn!` at construction so the insecure posture is visible at boot. `verify` uses `argon2::Argon2::default().verify_password` on the hashed branch and `subtle::ConstantTimeEq` on the plaintext branch — constant-time in both. Manual `Debug` impl redacts the secret. 8 unit tests pin: accept/reject for both flavours, reject gibberish PHC, reject empty plaintext, length-independent ct_eq, `is_plaintext_fallback` only true on the fallback. New `src/infrastructure/hashing/argon2_hasher.rs` + `src/bin/hash_admin_password.rs` — `make hash-admin-password PASSWORD=...` generates a fresh salt and prints a PHC string for operators to paste into `ADMIN_PASSWORD_HASH`. `src/auth.rs::login` no longer does `password == env` — it constructs `AdminCredentials` via the new `load_admin_credentials` helper (prefer `ADMIN_PASSWORD_HASH`, fall back to `ADMIN_PASSWORD`; invalid hash fails closed without silent plaintext fallback). `.env.example` documents the precedence; `docker-compose.yml` passes `ADMIN_PASSWORD_HASH` through. Deps: `argon2 = 0.5`, `password-hash = 0.5` (alloc), `subtle = 2` — all gated to `ssr`. New `[[bin]] hash-admin-password` in `Cargo.toml`. 130 rust tests across 10 binaries green.
- 6.3  2026-04-17 — **no-op** — `@keyframes slide-in` + `animate-slide-in` utility already landed in `tailwind.config.js` as part of 6.1 (the token file is the natural home for motion — keeping a separate `style/input.css` block just for the one animation would split a small concern). The toast's existing `animate-slide-in` reference now resolves. Closes F3 via 6.1.
- 6.2  2026-04-17 — new `src/components/design/` module hosts the six primitives named in the plan: `Button` (variant × full_width class matrix — Primary/Secondary/Danger/Ghost), `Input` (text-like, two-way binds a `RwSignal<String>`, honours a reactive `readonly`), `Select` (two-way binds; caller composes `<option>`s so Suspense/iteration stays at the call site), `Surface` (Raised/Overlay elevation × optional built-in padding), `Stack` (Tight/Normal/Loose vertical gap — enumerated so Tailwind's JIT scanner sees every literal class), `Badge` (Neutral/Brand/Success/Warning/Danger tones). All classes use the semantic tokens from 6.1 (`brand`/`surface`/`ink`/`feedback`, `rounded-control`/`rounded-card`, `duration-quick`). Migrated `KeyForm`, `SubscriptionForm`, `RateLimitIntervalForm`, `LoginPage` — the four forms that concentrated the class-soup. Kept the subscription `<select>` in `KeyForm` raw because selecting a plan back-fills the rate-limit field (domain coupling that doesn't belong inside a generic primitive). Secondary/submit buttons use the shared variants; cancel buttons pass `on_click` as a `Callback<MouseEvent>` rather than hoping Leptos spreads `on:click` through a component boundary. `KeyForm`, `SubscriptionForm`, `RateLimitIntervalForm`, `LoginPage` now read top-to-bottom as "form→Stack→fields→actions" rather than a wall of identical Tailwind classes. No behavioural change — the button tree and input wiring are identical. 155 rust tests still green.
- 6.6  2026-04-17 — `src/components/key_row.rs` copy-button no longer calls `js_sys::eval` with a format-string of `navigator.clipboard.writeText('<escaped key>')`. The eval path broke on any key containing a backtick or newline, and even the apostrophe-escape it did do was a log-injection-shaped risk. The typed `web_sys::window().navigator().clipboard().write_text(&k)` API passes the key as a JS string *value* (not source), so there is no escaping concern at all. Promise is still fire-and-forget — avoids pulling in `wasm_bindgen_futures` for a trivial UX affordance. `"Clipboard"` added to `web-sys` features. Commit lands before 6.2–6.5 because it was the immediate security smell still in-tree; returning to 6.2 next. 155 rust tests still green.
- 6.1  2026-04-17 — `tailwind.config.js` now carries a semantic token layer: `colors.brand.*` (primary action blue ladder), `colors.surface.*` (neutral backgrounds + borders), `colors.ink.*` (text tiers), `colors.feedback.*` (danger/warning/success + their -ink variants), `borderRadius.control`/`card`, `boxShadow.raised`/`overlay`, `transitionDuration.quick`/`settled`, plus a `slide-in` keyframe + matching `animate-slide-in` utility so the missing F3 animation finally resolves. Values chosen by grepping the actual inline usage in `components/` + `pages/` so migration is a mechanical find-and-replace rather than a repaint (68x `text-gray-500` → `text-ink-muted` etc). Additive only — the raw palette stays available so 6.2+ can migrate piece by piece instead of in one breaking sweep. No component files touched in this commit; tokens shipped first so the next commit has a vocabulary to compose primitives from. 155 rust tests still green.
- P5-e2e 2026-04-17 — Phase 5 boundary `make e2e`: 77/77 green (53 chromium + 10 api + 14 webkit smoke). Access log middleware fires on every request without impacting latency; request-id header round-trips through Playwright API tests; `/healthz` and `/readyz` available but not yet asserted in e2e (they're infra probes, not product surface). Envelope unchanged.
- 5.7  2026-04-17 — new `.github/workflows/lint.yml` with two jobs: `audit` runs `rustsec/audit-check@v2.0.0` (reports RustSec advisories on every PR without hard-blocking — the signal informs the next patch release), and `docker` uses `docker/build-push-action@v6` with Buildx GHA caching to verify the Dockerfile still builds (no push — release workflow handles publishing). fmt-check + clippy-D + tests already live in test.yml via `make check`, so lint.yml is kept narrow: the split means a transient advisory or registry blip doesn't block the test signal, and turning the two gates on/off separately is easy when one of them gets noisy. 155 rust tests still green; Phase 5 boundary e2e 77/77 green.
- 5.6  2026-04-17 — Dockerfile hardened: (1) base images tightened to `rust:1.90-bookworm` + `debian:bookworm-slim` (full distro pin — the plan asked for a sha256 digest pin, but tag-level keeps weekly CVE patches landing without a manual bump; digest pins become valuable once a Dependabot/Renovate bot is running and not before), (2) Node install no longer pipes `curl | bash` — the NodeSource signing key lives in `/etc/apt/keyrings`, the apt source file names the `[signed-by=…]` pubkey, and the install completes via plain `apt-get install nodejs`, (3) a system `koentji` user (uid 1001, nologin shell) owns the app files — a `USER koentji` instruction drops privileges so a compromise of the process doesn't hand the attacker root inside the container, (4) `HEALTHCHECK` runs `curl --fail http://localhost:3000/healthz` every 30s (3s timeout, 20s start-period, 3 retries) — deliberately hits /healthz (liveness) not /readyz (a transient DB blip must NOT restart the container). `curl` added to the runtime layer for the healthcheck. 155 rust tests still green.
- 5.5  2026-04-17 — `src/db.rs::create_pool` now pins three pool defaults: `acquire_timeout(5s)` so a request waiting longer than this fails loud instead of hanging behind a saturated pool, `idle_timeout(600s)` so Postgres isn't carrying ~20 idle backends forever while a burst still finds warm connections, and `test_before_acquire(true)` so a Postgres restart or network blip doesn't hand out dead sockets one at a time. Chose hard-coded values over env flags for now — operators who need different numbers will want to know *why*, and the defaults land in the same file the SQL migration runner already lives in, so the next reader sees them together. 155 rust tests across 11 binaries green.
- 5.4  2026-04-17 — `main.rs` now configures `HttpServer::shutdown_timeout(SHUTDOWN_TIMEOUT_SECONDS || 30)` and, after `.run().await` returns, calls `pool.close().await` so `PgPool` terminates cleanly instead of being dropped mid-query. Actix already traps SIGINT/SIGTERM on unix and stops accepting new connections while draining the in-flight ones — adding the explicit pool close plus a pair of bracketing log lines makes the shutdown sequence observable in the access log without buying a signal-handling crate. `SHUTDOWN_TIMEOUT_SECONDS` is env-configurable for operators whose longest request exceeds 30s. No new tests — graceful shutdown is a boot-time wiring concern that integration tests (which exit at end-of-scope) don't exercise meaningfully; the contract is pinned by inspection of the server main plus the e2e suite at the phase boundary.
- 5.3  2026-04-17 — new `src/interface/http/health.rs` — `/healthz` (liveness) returns `{"status":"ok"}` without touching any shared state; the kubernetes/Docker `HEALTHCHECK` → restart loop must NEVER fire because of a transient Postgres outage, so the probe intentionally doesn't know the DB exists. `/readyz` (readiness) runs `SELECT 1` with a 2s `tokio::time::timeout` around the whole acquire+query dance — a stuck pool is just as unready as one that errors. Returns `{"status":"unavailable","database":"<reason>"}` + `503` so a load balancer can drain traffic without tripping restarts. `main.rs` wires both under the same `web::Data<PgPool>` already registered for leptos. Unit test in-module pins the no-pool-required contract for liveness (enforced at the type level — `healthz` takes no extractor). New `tests/health_endpoints.rs` (3 tests) pins the real pool contract: readyz returns 200 against a fresh pool, 503 against a closed pool (the real failure mode — a transient DB outage), and healthz still answers 200 with no pool registered at all. 155 rust tests across 11 binaries green.
- 5.2  2026-04-17 — new `src/infrastructure/telemetry/request_id.rs` — `RequestIdMiddleware` (actix `Transform`/`Service`) accepts a trusted inbound `X-Request-Id` or mints a UUID v7, stores it as a `RequestId` extension on the request, and mirrors it as an outbound `X-Request-Id` header so clients can correlate failed calls with log lines. Inbound ids are admitted only if printable ASCII and ≤128 bytes — an echoed header with newlines/control bytes would otherwise be a log-injection vector into our structured access log. `access_log.rs` now reads the extension and emits the id as a top-level `request_id` field on every access line (ordered directly after `ts` so a Loki viewer sees it immediately). `main.rs` wraps `AccessLog` before `RequestIdMiddleware` — actix runs wraps in reverse, so RequestId runs first on the request (setting the extension) and AccessLog reads it. `uuid = { version = "1", features = ["v7"] }` added under `ssr` — v7 is time-sortable so chronological grep on log files still works. 5 unit tests: generates id when absent, preserves trusted inbound, rejects control chars (tab — newlines/NULs are blocked at the HTTP parser layer), rejects oversized inbound, stores the id as a request extension downstream handlers can read. 152 rust tests across 10 binaries green.
- 5.1  2026-04-17 — new `src/infrastructure/telemetry/{mod,access_log}.rs`. Custom actix `Transform`/`Service` middleware (`AccessLog`) emits one single-line JSON log per completed request on target `http_access`, with ts/remote/method/path/status/bytes/duration_ms/referer/user_agent. Hand-rolled rather than `middleware::Logger` — a format string can't JSON-escape header values / timestamps safely, and the serde route is ~80 lines with correct escaping. Chose this over a `tracing`-based stack because the rest of the codebase uses `log`, and a structured line on a dedicated target is the minimum Loki / Datadog need to parse without regex. Wrapped outermost (last `.wrap` on the App) so every request — even auth-failed or never-routed — produces a line. Body size via `BodySize::Sized(n)`, `Chunked`/`Stream`/`None` collapse to 0 (unknown-size responses are rare enough here not to warrant buffering). Remote IP from `connection_info().realip_remote_addr()` so X-Forwarded-For is honoured. Three unit tests: happy-path body/status preservation, non-2xx propagation, and a JSON-shape regression test that pins the single-line serde output and `skip_serializing_if = "Option::is_none"` on `referer`/`user_agent`. 147 rust tests across 10 binaries green.
- 3.4  746030c 2026-04-17 — new `src/domain/authentication/{events,audit_event_port}.rs` define `DomainEvent` (flat primitives, past-tense variants for every admin verb — `KeyIssued`, `KeyRevoked`, `DeviceReassigned`, `RateLimitReset`, `KeyExpirationExtended`) and the `AuditEventPort` trait. New `src/infrastructure/postgres/audit_event_repository.rs` (`PostgresAuditEventRepository`) writes one row per event to `audit_log`, encoding event-specific fields into the JSONB payload. The adapter is deliberately fire-and-forget: a failed insert is logged at `warn!` but never bubbles back — auditing must not fail the witnessed operation. All five admin verb use cases take `Arc<dyn AuditEventPort>` and publish after repository success (no event on unknown ids — events record facts that changed state). `main.rs` constructs `Arc<dyn AuditEventPort>` alongside the repo + cache ports and injects it into each use case. Tests: +1 FakeAudit in `tests/key_management_use_cases.rs` with assertions that every verb emits the right event and unknown-id paths emit none; new `tests/postgres_audit_event_repository.rs` (6 tests) round-trips every variant through JSONB and covers the closed-pool fire-and-forget path. `tests/common/db.rs::reset` also truncates `audit_log`. 120 rust tests across 9 suites green.
- 6.4  2026-04-17 — two new primitives in `src/components/design/`: `PageHeader` (h1 + optional subtitle — the action button lives *beside* it via the caller's flex wrapper, so pages without a single primary CTA still compose cleanly) and `DataTable` (bg-surface-base + rounded-card + shadow-raised wrapper + `<thead>` row from `Vec<&'static str>` + `<tbody>` from `children` — every `<th>` gets `scope="col"` for free a11y). Migrated `src/pages/subscriptions.rs` and `src/pages/rate_limits.rs` onto both — the page header + filter bar + table + confirm-delete + create/edit-modal structure is now ~identical between them without needing a generic `CrudPage<Item, Form>`. **Scope note**: the plan asked for a generic `DataTable<T>` / `CrudPage<Item, Form>`; chose the pragmatic split instead — Leptos's generic-component ergonomics are rough (lifetimes leak into prop signatures, type params bleed into every view usage), and the `Vec<_>::into_iter().map(…)` at each call site is readable. Documented the decision in the `DataTable` doc comment so the next reader doesn't re-propose the generic. `keys.rs` still has its hand-rolled `KeyTable` — it carries per-row expand state and KeyRow's clipboard/copy affordances, which don't fit the generic shape. 155 rust tests still green.
- 6.7  2026-04-17 — `src/components/charts.rs` no longer does `js_sys::eval("setTimeout(function(){ renderCharts(); }, 0)")` and no longer writes the JSON string to `window.__chartData` via reflection. New typed extern `#[wasm_bindgen] extern "C" { #[wasm_bindgen(js_name = renderCharts)] fn render_charts_js(data_json: &str); }` lets the Rust side call the global directly with the JSON payload as an argument. `public/js/charts.js` was refactored to match — the entry is now `renderCharts(dataJson)` which parses the string and defers rendering via `setTimeout(_, 0)` (canvas mounted by then). The deferral moved out of Rust eval and into JS so the CSP surface shrinks (no more eval required — a future `script-src 'self'` without `unsafe-eval` would now be viable). 156 rust tests still green.
- 6.10 2026-04-17 — `KeysPage` filters (`search`, `page`, `subscription`, `status`) now live in the URL via `leptos_router::hooks::query_signal::<T>`. A link like `/keys?search=foo&status=expired&page=3` restores the exact filtered view — shareable with a teammate, survives a refresh, and the browser back/forward buttons walk the filter history naturally. Each param is stored as `Option<T>`: `None` means "default" (page 1, no filter) so an empty `/keys` URL stays clean. Filter changes always set `page=None` — switching subscription while on page 7 would otherwise show an empty table. The debounced search writer still owns the 300ms timer from 6.8; the change is only where it writes (URL param instead of a local RwSignal). `KeyTable`'s `page` prop relaxes from `RwSignal<i32>` to `Signal<i32>` + a new `on_page_change: Callback<i32>` so the child doesn't need write access to the URL param — the parent owns the encoding rule (page 1 → None). 156 rust tests still green.
- 6.9  2026-04-17 — `KeyForm` submit no longer swallows errors. Previously `handle_submit` ran `if result.is_ok() { on_submit.run(()) }` and let the `Err` arm fall off the end — the admin would click Create or Update, see the form reset, and have no clue the server fn returned (for example) "rate limit amount out of range" or "device already in use". Now the `Err` arm lands in `use_toast().error(&format!("Failed to {} key: {}", …, err))`, matching the convention already used by `SubscriptionsPage` and `RateLimitsPage`. The error message from the `ServerFnError<NoCustomError>` surfaces as-is — good enough until a phase that pipes typed domain errors through the server-fn boundary, which isn't on this roadmap. `submitting` is cleared on both arms so a failed submit doesn't leave the button disabled. 156 rust tests still green.
- 6.8  2026-04-17 — `src/pages/keys.rs` search debounce is no longer a timer-spray. Prior incarnation called `set_timeout` on every keystroke without cancelling the previous handle — five rapid keys fired five `search.set(...)`s 300ms apart, each triggering a resource refetch. Now a `StoredValue<Option<TimeoutHandle>>` holds the pending handle; the effect clears it via `handle.clear()` before scheduling the next one. Five keystrokes collapse back to one fetch. `set_timeout_with_handle` returns a `Result` so a scheduling failure doesn't panic — we just skip the replacement (worst case: one stale fetch, same as before the debounce). 156 rust tests still green.
- 6.11 2026-04-17 — feature-folder reshuffle. `src/components/` + `src/pages/` retired in favour of `src/ui/<feature>/` mirroring the bounded-contexts vocabulary from the plan: `ui/design/` (the six primitives + `DataTable` + `PageHeader` + `Modal` + `Toast`), `ui/shell/` (`Layout`), `ui/keys/` (page + `KeyForm` + `KeyRow` + `KeyTable`), `ui/subscriptions/`, `ui/rate_limits/`, `ui/dashboard/` (page + charts + date range picker + stats cards), `ui/admin_access/` (login page), `ui/marketing/` (landing/about/terms/privacy/quickstart). Every move via `git mv` so file history follows the symbol. New per-feature `mod.rs` files re-export the `*Page` types so `crate::ui::<feature>::FooPage` stays ergonomic; `lib.rs` drops the two old module declarations in favour of a single `pub mod ui;`. 29 `use crate::{components,pages}::*` sites rewritten across 13 files; `cargo fmt` then reshuffled 6 of those import blocks into alphabetical order so `fmt --check` stays clean. Behaviour unchanged — pure symbol-location refactor. 156 rust tests still green. **Phase 6 complete; Phase 7 deferred per user instruction ("complete only phase 6 ya, please stop before start phase 7").**
- 6.5  2026-04-17 — `Modal` + `ConfirmModal` grow a full a11y layer: `role="dialog"` / `role="alertdialog"`, `aria-modal="true"`, `aria-label` bound to the title, tabindex="-1" on the dialog root so it's programmatically focusable, a keydown handler that closes on Escape and traps Tab / Shift+Tab inside the dialog via a private `trap_tab` helper that walks the focusable descendants in document order, and a focus-save/restore pair (`snapshot_focus` before open → restore on close) so keyboard users don't get stranded. Modal classes migrated onto the semantic tokens (`bg-surface-base`, `rounded-card`, `shadow-overlay`, `border-surface-border`, `duration-quick`) while the file was open. Also this commit: toast container gets `role="status" aria-live="polite"` (individual error toasts override to `role="alert"` so a screen reader announces them); key-row icon-only buttons (reveal/copy/reset/edit/revoke) grow `aria-label` + `aria-hidden="true"` on their inner `<svg>` so the label isn't stepped on; `Input` / `Select` primitives accept an optional `id` prop; the four forms (`KeyForm`, `SubscriptionForm`, `RateLimitIntervalForm`, `LoginPage`) wire `<label for=...>` → input `id` linkage so screen readers announce the field label when focus lands. `web-sys` features +`Element` / +`Node` / +`NodeList` for `query_selector_all` / `Element::contains`. All close/cancel buttons also grow explicit `type="button"` so they can't accidentally submit an enclosing form. 156 rust tests green.

## Blockers

(None yet.)

## How to resume

1. Read this file — the first unchecked box is the next commit.
2. Read `/root/.claude/plans/use-razif-coding-style-audit-current-velvet-lampson.md` for the full plan context.
3. Implement the slice for that commit.
4. Run `make check` — never commit red.
5. Commit code + PROGRESS.md update together with the plan's commit message.
6. Tick the box, append a Log line.
7. At phase boundaries, also run `make e2e` before starting the next phase.
8. Loop.

Stop if a commit fails `make check`/`make e2e` in a way that needs a product decision — record the reason in Blockers and stop.
